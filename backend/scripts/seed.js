// Seed script runner
// Usage: node scripts/seed.js

const path = require('path');
const seedData = require('../data/seedData');

// This will be handled by the seedData.js file itself
console.log('Starting database seeding...');
